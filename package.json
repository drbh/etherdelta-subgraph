{
  "name": "Etherdelta",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ drbh/Etherdelta",
    "create-local": "graph create --node http://localhost:8020/ drbh/Etherdelta",
    "remove-local": "graph remove --node http://localhost:8020/ drbh/Etherdelta",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 drbh/Etherdelta"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.9.0",
    "@graphprotocol/graph-ts": "^0.9.0"
  }
}
